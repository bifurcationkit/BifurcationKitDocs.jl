# Bordered Arrays

Here is some information on a composite type which is used all over the package to hold a couple (array, array) or (array, scalar) while implementing the methods described in [Required methods for custom arrays](@ref Required-Methods). 


```@docs
BorderedArray
```